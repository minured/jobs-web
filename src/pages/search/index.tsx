import { Input } from "@tarojs/components";
import styles from "./index.scss";

const Search = () => {
    return (
        <div className={styles.P_search}>
            <Input /> Search
        </div>
    );
};

export default Search;
